using System;

namespace VL.Gaming.Study.Algorithms.StringProcessing
{
    /// <summary>
    /// LevenshteinDistance,字符串编辑距离算法
    /// 时间复杂度 ？？？
    /// </summary>
    public class Sample_LevenshteinDistance
    {
        public int[] Process(int[] array)
        {
            if (array == null || array.Length == 0)
                return array;
            var result = new int[array.Length];
            //TODO
            return result;
        }
    }
}
